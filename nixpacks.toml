[variables]
NIXPACKS_NODE_VERSION = "22"
NIXPACKS_PHP_VERSION = "8.4"

[phases.release]
cmds = ["php artisan migrate --force"]
dependsOn = ["build"]  # <-- Baris ini memaksa migrasi berjalan SETELAH dependencies selesai diinstal dan di-build

